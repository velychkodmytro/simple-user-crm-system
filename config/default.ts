const config = {
    myDatabase: {
        database: 'user_system',
        username: 'admin',
        password: 'password',
        host: 'localhost',
        dialect: 'mysql',
        PORT: 8000,
    },
};

module.exports = config;
