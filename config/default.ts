const config = {
    myDatabase: {
        database: 'user_system',
        username: 'admin',
        password: 'password',
        host: 'localhost',
        dialect: 'mysql',
        port: 3306,
    },

    api: {
        port: 8000,
    },
};

module.exports = config;
