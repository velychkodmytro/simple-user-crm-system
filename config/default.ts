// import config from 'config';

const config = {
    database: {
        database: 'user_system',
        username: 'root',
        password: 'password',
        host: 'localhost',
        dialect: 'mysql',
        PORT: 3306,
    },
};

export default config;
